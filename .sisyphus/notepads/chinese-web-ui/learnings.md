- Added a new lightweight copy foundation at `web/src/lib/copy/` with one shared glossary module plus focused surface modules for shell, new-task, task-detail, workspace, and environment-status.
- Shared label/fallback helpers are plain constants/functions only, so later tasks can import them without introducing provider/context/runtime i18n machinery.
- `web/src/components/AppShell.tsx` now consumes `APP_SHELL_COPY` directly for the header, primary nav label, side-rail reserved panel, and future workspace copy while preserving the existing layout classes, `NavLink to="/"`, and query wiring.
- `web/src/pages/NewTaskPage.tsx` now follows the same pattern by importing `NEW_TASK_COPY` and `NEW_TASK_PROCESSING_OPTIONS`, which keeps the Chinese intake copy centralized while preserving the existing form state keys, `source_url` mutation payload, submit test IDs, and `/tasks/<id>` navigation.
- Task 3 wired `TaskDetailPage.tsx` to `TASK_DETAIL_COPY` and moved stage/status rendering onto shared glossary-backed helpers while preserving `getPollingInterval()` values, route params, and query refetch triggers exactly as before.
- `web/src/lib/types.ts` now treats summary translation as an explicit-map boundary only: empty summaries use the shared Chinese waiting fallback, known codes like `translation_failed` map to Chinese, and all other backend-provided summary/free-text values pass through unchanged for later surfaces such as workspace reasons.
- Task 4 refactored `web/src/pages/WorkspacePage.tsx` to consume `WORKSPACE_COPY`, `getWorkspaceDownloadLabel()`, and `formatWorkspaceReason()` for all frontend-owned workspace chrome while preserving raw backend `no_candidates` text, transcript content, existing export/download wiring, and artifact kind/url behavior.
- `web/src/components/EnvironmentStatusCard.tsx` now follows the same centralized-copy pattern as the other refactored surfaces by sourcing eyebrow/title, badges, metadata labels, section headings, and frontend-owned explanatory copy from `web/src/lib/copy/environmentStatus.ts`, while still rendering backend-provided warning strings and raw `capabilities.issues[].message` text unchanged.

- Task 5 updated the five targeted Vitest/smoke files to assert centralized Chinese frontend-owned shell, intake, detail, workspace, and environment-status copy while preserving route navigation to `/tasks/<id>`, 3s/15s polling assertions, stable `data-testid` usage, and raw backend free-text expectations such as runtime issue payloads and workspace `no_candidates` text.
- Task 6 updated the four Playwright specs in `web/e2e/` to assert the shipped Chinese shell/intake/detail/workspace copy while preserving `/tasks/<id>` routing, mocked backend payload prose, and workspace export flows; ambiguous `下载双语字幕` assertions needed `exact: true` to avoid strict-mode collisions with the separate `下载双语字幕 JSON` link.
- F2 code-quality cleanup kept scope narrow: `WorkspacePage.tsx` now sources the frontend-owned missing-`candidate_id` export error from `web/src/lib/copy/workspace.ts`, `TaskDetailPage.test.tsx` asserts `翻译失败` inside the readable failure-summary panel instead of counting duplicate text nodes, and `WorkspacePage.test.tsx` uses exact-name download-link selectors to stay robust against `下载双语字幕` / `下载双语字幕 JSON` collisions.
