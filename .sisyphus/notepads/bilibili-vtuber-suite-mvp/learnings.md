- 2026-04-25: Task 1 bootstrap works with a minimal FastAPI root endpoint plus Vitest smoke shell; `uv run --project backend ...` succeeds once backend package metadata includes `README.md` and an importable `app` package.
- 2026-04-25: Docker Compose v2.30 validates GPU intent when `gpus` uses the explicit mapping form (`driver`, `count`, `capabilities`) instead of a scalar shortcut.
- 2026-04-25: Parent verification expects `uv` to be runnable from a PATH directory; a repo-local shim at `/home/drm/workfile/tts/.bin/uv` reliably bridges to the installed ELF at `/home/drm/workfile/tts/.local/uv`.
- 2026-04-25: The workflow must not ignore the full `.sisyphus/` tree because plans/specs/notepads are part of the orchestration state; only narrow generated evidence paths should be ignored.
- 2026-04-25: FastAPI route mutations that only `flush()` SQLModel changes are not durable across subsequent requests; Task 2 needed explicit `session.commit()` in mutating task endpoints so create/retry responses matched persisted state.
- 2026-04-25: For env-isolated backend tests, clearing the cached SQLModel engine between cases is cleaner than re-importing `app.*` modules; reloading model modules causes duplicate table registration noise while engine-cache reset preserves fresh DB/data paths without metadata churn.
- 2026-04-25: Local no-env verification in this workspace cannot rely on absolute `/data` being writable, so backend defaults should prefer `APP_DATA_DIR` and otherwise fall back to the repo-local `data/` directory when `/data` is unavailable.
- 2026-04-25: Task 3 downloader/media-prep tests that mutate SQLModel rows inside `session_scope()` need to read scalar values before the context exits; otherwise commit-time expiration causes detached-instance assertion noise instead of meaningful failures.
- 2026-04-25: For direct service calls wrapped in `session_scope()`, classified stage failures that should remain durable after an exception need an explicit `session.commit()` before re-raising, or the rollback will erase the failed stage summary/status.
- 2026-04-25: Task 4 ASR/subtitle tests are stable when the WhisperX provider is injected behind a loader helper instead of importing the package at module import time; this keeps repo-local pytest independent of heavyweight model/provider installs while still exercising the real service contract.
- 2026-04-25: Task 4 needs to write any raw WhisperX alignment payload to disk before validating segment timestamps, so alignment-failure retries still preserve the raw JSON artifact for operator debugging.
- 2026-04-25: Task 4 packaging was fixed by declaring the runtime `whisperx` dependency in backend metadata; lazy ASR loading and provider-free tests still pass once the backend environment installs the provider.
- 2026-04-25: Task 5 translation tests stay deterministic when the local Hugging Face provider keeps `transformers`/`torch` imports behind loader helpers and the stage service injects the provider via a builder function, mirroring the lazy-load pattern used for WhisperX.
- 2026-04-25: The bilingual subtitle contract is stable when translation reuses the existing `SubtitleSegment` structure and only adds `translated_text` in the bilingual JSON plus a second rendered line in `subtitles.zh-ja.srt`; this preserves segment ids/timestamps without merge or re-split logic.
- 2026-04-25: Task 6 highlight analysis stays deterministic when it prefers persisted `subtitles.zh-ja.json`/`asr-segments.json`, consumes scene-boundary JSON from `tasks/{task_id}/work/`, and writes a single `highlight-candidates.json` artifact containing candidate payloads plus full score/source-signal breakdowns.
- 2026-04-25: The zero-candidate highlight path must still end with stage `success`; persisting an empty candidate artifact with a human-readable `no_candidates` explanation avoids losing the downstream contract while keeping retries reserved for real failures.
- 2026-04-25: Task 7 clip export stays deterministic when the export filename is derived from validated start/end milliseconds (`clip-########-########.mp4`) rather than transient candidate ordering; repeated exports of the same task/range naturally converge on one stable output path and one updatable artifact row.
- 2026-04-25: Report generation is simplest and most durable when it reads from existing `Task`, `TaskStage`, and `Artifact` rows instead of reparsing every stage file directly; that keeps the Markdown report aligned with the backend’s source of truth for timings, retries, model metadata, and exported clip records.
- 2026-04-25: Cleanup for this MVP should be conservative and scoped to `work/`: preserving subtitle files plus manifest/metadata JSON avoids breaking retries, reports, and later UI downloads, while deleting transient audio/alignment scratch files still shrinks the task footprint after success.
- 2026-04-25: Task 8 orchestration is simplest when the worker keeps claiming the existing single durable TaskJob row, but the new task runner owns canonical stage sequencing and commits after every stage so retries resume from the job's current stage without recomputing upstream successes.
- 2026-04-25: Task 9 frontend polling is easiest to keep correct when the cadence is derived from one exported helper (`3s` for non-terminal states, `15s` for terminal states) and that same helper is asserted directly in unit tests instead of relying on brittle fake-timer integration against React Query internals.
- 2026-04-25: Playwright coverage for this web shell can stay fully repo-local and deterministic by routing the backend API URLs (`http://127.0.0.1:8000/api/...`) at the browser layer; that verifies the page-oriented UX without needing a live backend process for Task 9 smoke scenarios.
- 2026-04-25: Task 10 frontend can stay on current backend contracts by loading subtitle/highlight artifact contents from artifact  values at the frontend layer and mocking those fetches in Vitest/Playwright; this keeps workspace rendering deterministic without inventing a new content endpoint.
- 2026-04-25: Task 10 frontend can stay on current backend contracts by loading subtitle/highlight artifact contents from artifact path values at the frontend layer and mocking those fetches in Vitest/Playwright; this keeps workspace rendering deterministic without inventing a new content endpoint.
- 2026-04-25: Task 11 docs had to follow the actual MVP route structure in `web/`, not a hypothetical `frontend/` app, with the workstation living inside `TaskDetailPage` at `/tasks/:taskId` and clip export remaining a manual POST after the pipeline marks `export` as skipped.
- 2026-04-25: Task 11 operator docs need to call out a real deployment caveat: the current backend image wires GPU, `/data`, and model-cache mounts correctly, but it still expects external media tools such as `BBDown`, `yt-dlp`, `ffmpeg`, and `ffprobe` to be present at runtime.
- 2026-04-25: Task 12 smoke coverage is most stable when it exercises `run_worker_iteration()` against a repo-local SQLite/db tempdir and monkeypatches `task_runner.STAGE_EXECUTORS`; that keeps the worker claim path real while avoiding any live downloader/ASR/media dependencies.
- 2026-04-25: Task 12 browser smoke is easiest to keep deterministic by merging submission/detail/workspace coverage into one Playwright flow spec that reuses the existing route-layer API mocks plus browser-fetched fixture paths for transcript, subtitle, highlight, report, and clip artifacts.
- 2026-04-25: Browser-facing artifact contracts cannot reuse the persisted filesystem `Artifact.path` values directly; the stable fix is to keep DB storage paths internal and serialize artifact `path` for the frontend as an app-backed `/api/tasks/{task_id}/artifacts/{artifact_id}/content/{filename}` route that the web client resolves against `VITE_API_BASE_URL`.
- 2026-04-25: F4 runtime fidelity is repaired cleanly by installing `ffmpeg`/`ffprobe` from Debian, `yt-dlp` via `pip`, and the published BBDown Linux zip in the backend image, then exposing all four commands through `/app/.bin` so the backend can keep its existing default binary names.
- 2026-04-25: Release smoke now matches the deployment acceptance intent more closely when it starts the Compose `api`/`web` services first, waits for live HTTP readiness, and verifies in-container tool resolution before running the existing pytest/Vitest/Playwright checks.
- 2026-04-25: Highlight-stage fidelity is preserved by resolving scene boundaries in this order: existing persisted scene artifact, real PySceneDetect provider against the ingested source video, then explicit subtitle-gap fallback persisted as `scene_like_boundaries_json` only when scene detection cannot run or returns no usable scenes.
- 2026-04-25: F4 scope-fidelity recheck now supports APPROVE: the backend image directly provisions `BBDown`/`yt-dlp`/`ffmpeg`/`ffprobe`, highlight analysis uses real PySceneDetect with persisted fallback only as a secondary path, artifact downloads are serialized as app-backed `/api/tasks/{task_id}/artifacts/{artifact_id}/content/{filename}` URLs, and `scripts/release_smoke.sh` validates Compose config, boots `api`/`web`, waits for HTTP readiness, and checks in-container tool resolution before offline smoke tests; the only fresh execution blocker was host Docker-daemon access, not repository scope fidelity.
- 2026-04-25: Second F4 rerun still supports APPROVE after the newest fixes: `scripts/release_smoke.sh` now explicitly starts `api worker web`, highlight candidate artifacts persist durable `candidate_id` values from real `ClipCandidate` rows, and task reports list real clip candidates from SQLite rather than invented frontend-only identifiers.
- 2026-04-25: Final MVP QA gate can rely on fresh fixture-driven Playwright evidence from `web/e2e/task-flow.spec.ts`, `web/e2e/workspace.spec.ts`, and `web/e2e/task-detail.spec.ts`; the combined run verified submit → canonical detail → workspace export, readable translation-failure recovery messaging with preserved artifacts, and zero-candidate UX while `pnpm --dir web build` still compiled cleanly.
- 2026-04-25: Highlight artifacts must be written after `ClipCandidate` rows are flushed so the persisted JSON can include durable `candidate_id` values that match the DB-backed export contract.
- 2026-04-25: Reporting now reads persisted ClipCandidate rows from the DB, so the task report clip-candidate section populates with real id/start/end/status/score data instead of staying empty.
- 2026-04-25: release_smoke.sh now boots api, worker, and web together; kept the change limited to the compose startup line and matching status message.
- 2026-04-25: After adding the dedicated Chinese manuals, the only documentation drift was `docs/README.md` still claiming `docs/` was a future scaffold; keeping a real bilingual index there prevents the English/Chinese doc sets from disagreeing about repo state.
