- 2026-04-25: Kept the backend scaffold intentionally tiny: `backend/app/main.py` only defines the importable FastAPI app and a root smoke route so Task 2 can add routers without undoing Task 1 code.
- 2026-04-25: Set the frontend baseline to Vite + React + TypeScript + Vitest + Playwright config, with Node 20 represented by the Dockerfile and `.nvmrc` while local verification stayed compatible with the host Node runtime.
- 2026-04-25: Compose uses a repo-local bind mount (`../data:/data`) to document the task artifact convention while keeping generated outputs outside Git.
- 2026-04-25: Chose the minimal Task-1-scoped `uv` fix: add an executable workspace shim in `.bin/uv` instead of changing required commands or depending on `.local/bin` existing.
- 2026-04-25: Added `*.tsbuildinfo` to `.gitignore` so local TypeScript incremental build metadata does not pollute product commits.
- 2026-04-25: Task 2 uses SQLModel tables for `Task`, `TaskStage`, `Artifact`, `ClipCandidate`, plus a minimal `TaskJob` queue table so task metadata stays in SQLite while files stay under the task artifact tree.
- 2026-04-25: Task creation seeds all canonical stages (`ingest`, `media_prep`, `asr`, `translation`, `highlight`, `export`, `report`) in `pending` immediately, and also seeds one durable pending `TaskJob` so later pipeline stages can claim work without any in-memory queue.
- 2026-04-25: The single-worker invariant is enforced by allowing only one `TaskJob` with `gpu_bound=true` and `status=running` at claim time; retry rewrites the existing job row to the requested failed stage instead of creating parallel jobs.
- 2026-04-25: Storage/runtime defaults prefer explicit `APP_DATA_DIR`, but local host runs fall back to the repo `data/` directory when `/data` is not writable so the same code works both in Compose (`../data:/data`) and in direct `uv run` verification.
- 2026-04-25: Task 3 keeps downloader-specific behavior behind normalized service contracts: ingest stores the selected downloader, fallback flag, auth-cookie presence, normalized source metadata, and raw video path, while media prep stores ffprobe JSON plus a deterministic mono 16kHz `work/asr-input.wav` artifact for downstream ASR.
- 2026-04-25: Cookie-path auth is accepted as an env-configured file path; BBDown receives the file contents as its `-c` cookie string while yt-dlp fallback receives the same path via `--cookies`, preserving one operator-facing config surface.
- 2026-04-25: Task 4 keeps the ASR contract deterministic under `tasks/{task_id}/work/`: raw WhisperX alignment is stored as `asr-alignment-raw.json`, normalized internal transcript JSON as `asr-segments.json`, and timed Chinese subtitles as `subtitles.zh.srt`.
- 2026-04-25: WhisperX integration is lazy-loaded and configured only through `APP_WHISPERX_*` settings so the service layer can classify missing provider/model, OOM, and alignment failures without coupling API routes or tests to the runtime package install state.
- 2026-04-25: Task 5 keeps translation behind `translation_provider.py` as the stable stage contract while `translation_hf.py` owns all Transformers/PyTorch details; the stage persists `subtitles.zh-ja.json` and `subtitles.zh-ja.srt` as translation artifacts under the existing task `work/` directory.
- 2026-04-25: Task 6 keeps highlight persistence split by purpose: `ClipCandidate` rows store the ranked scene-window basics needed for durable task state, while `work/highlight-candidates.json` stores the richer reason codes, source signals, score breakdowns, default clip ranges, and `no_candidates` explanation for later UI/report consumers.
- 2026-04-25: Task 7 persists confirmed clip exports as `Artifact` rows on the existing `export` stage (`kind="clip_export"`) and marks the selected `ClipCandidate` row as `exported`, avoiding any new durable export table while still giving the API/reporting layer a clean SQLite-backed record of clip metadata.
- 2026-04-25: The backend write contract for `POST /api/tasks/{task_id}/clips` takes a durable `candidate_id` plus optional `start_s`/`end_s` overrides, validates those bounds against the persisted media probe duration, and runs a synchronous ffmpeg export to keep the MVP confirmation flow simple for later UI wiring.
- 2026-04-25: Task 8 introduces `backend/app/services/task_runner.py` as the sole backend orchestrator: ingest can backfill `Task.source_video_id` for dedupe, export is intentionally marked `skipped` until a user confirms a clip, and the durable TaskJob row advances its `stage_name` checkpoint after each successful/skipped stage instead of spawning per-stage queue rows.
- 2026-04-25: Task 9 establishes a minimal web design system in `web/src/styles.css` and an `AppShell` three-column layout so the MVP remains a single-task workstation today while explicitly reserving left/right regions for the future task list and subtitle/highlight workspace.
- 2026-04-25: Task 9 keeps the New Task toggles UI-local only; translation/highlight/export options are visible and interactive in the form, but the submission contract still posts only `source_url` so the frontend matches the existing backend API instead of inventing unsupported task options.
- 2026-04-25: Task 10 keeps the workspace on  by mounting a dedicated  section inside ; this reuses Task 9 polling/detail ownership and avoids a separate route or shell-data plumbing pass in MVP.
- 2026-04-25: Task 10 keeps the workspace on /tasks/:taskId by mounting a dedicated WorkspacePage section inside TaskDetailPage; this reuses Task 9 polling/detail ownership and avoids a separate route or shell-data plumbing pass in MVP.
- 2026-04-25: Task 11 keeps Compose aligned with the single-worker MVP by reusing the backend image for both `api` and `worker`, sharing `../data:/data` plus `../data/model-cache:/models`, and exposing a `VITE_API_BASE_URL` override so LAN clients can point the browser at the host API without changing product code.
- 2026-04-25: Task 12 adds a backend smoke test around `run_worker_iteration()` instead of a narrower pipeline helper so the canonical job-claim contract stays covered end-to-end while the stage bodies remain offline-safe stubs.
- 2026-04-25: Task 12 keeps failed-translation browser evidence repo-local via a standalone seed_failure_fixture.py script that writes task detail/stages/logs/artifact manifests plus bilingual subtitle fixture files, while release_smoke.sh stays honest by verifying backend smoke, frontend tests/build, and the targeted Playwright flow instead of pretending to exercise live downloader/media-tool runtime dependencies.
- 2026-04-25: Final Verification Wave F4 artifact serving is fixed by exposing a real FastAPI file route at `/api/tasks/{task_id}/artifacts/{artifact_id}/content/{filename}` and serializing API artifact `path` fields to that route, while preserving the persisted SQLite/storage `Artifact.path` field as the backend-only filesystem source of truth for downstream stages.
- 2026-04-25: The backend Docker image now owns the MVP media toolchain directly and resolves it through `/app/.bin` shims, so `BBDown`, `yt-dlp`, `ffmpeg`, and `ffprobe` are available in both `api` and `worker` without changing backend settings defaults or pre-downloading any model weights.
- 2026-04-25: `scripts/release_smoke.sh` now boots only the Compose `api` and `web` services before the test/build flow so the script exercises real container startup and API health while avoiding nondeterministic worker side effects from the shared repo-local `data/` mount.
- 2026-04-25: The highlight stage now owns scene-boundary generation under the existing `Artifact` model: successful provider output is persisted as `scene_boundaries_json` in `work/scene-boundaries.json`, while safe subtitle-gap fallback is persisted as `scene_like_boundaries_json` in `work/scene-like-boundaries.json`, and heuristic ranking continues to score whichever `SceneWindow` set was resolved.
