- 2026-04-25: No unresolved Task 1 product-code problems. Operational note: future local Docker CLI checks in this workspace need `PATH=/home/drm/workfile/tts/.bin:$PATH` unless system Docker is installed.
- 2026-04-25: No unresolved Task 1 product-code problems from this fix pass. Operational note: verifiers should prepend `/home/drm/workfile/tts/.bin` to PATH when relying on workspace-local `uv` and `docker` shims.
- 2026-04-25: No blocking unresolved Task 2 product-code problems. Follow-on note for Tasks 3 and 8: the current `TaskJob` row is intentionally a durable single-job placeholder, so downstream orchestration should update that row/stage state rather than introducing a competing in-memory or parallel job model.
- 2026-04-25: Review gate F2 found a blocking MVP workflow gap: `backend/app/services/highlight_scoring.py` emits candidate payloads without `candidate_id`, `backend/app/services/highlights.py` persists those payloads unchanged into `highlight-candidates.json`, and `web/src/pages/WorkspacePage.tsx` disables export when `candidate_id` is absent. The mocked frontend tests invent `candidate_id`, so the real backend→workspace→clip-export path is currently broken.
- 2026-04-25: Review gate F2 found incomplete report logic in `backend/app/services/reporting.py`: the `## Clip Candidates` section is stubbed and always renders empty even though `ClipCandidate` rows exist. Current reporting tests pass because they never assert candidate coverage.
- 2026-04-25: Review gate F2 found release-smoke drift in `scripts/release_smoke.sh`: it only starts `api` and `web`, not `worker`, and it seeds a failure fixture directory that is not wired into the current Playwright flow. That means the script can pass without validating the worker service or the intended failure-fixture path.
