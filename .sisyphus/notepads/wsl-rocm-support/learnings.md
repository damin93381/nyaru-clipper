
2026-04-26
- Task 1 split the backend dependency surface into a neutral base export plus checked-in `linux-cuda` and `wsl-rocm` overlay artifacts.
- Keeping accelerator packages in `backend/project.dependencies` causes the exported base artifact to reintroduce torch/whisperx through the local `./backend` package reference, so the base package metadata had to stay accelerator-neutral.
- `uv pip compile` was a better fit than `uv export --locked` for artifact generation here because it can generate the neutral and profile overlays directly from declarative inputs without requiring the install wrappers to infer dependency trees at runtime.
- The WSL ROCm overlay needed `--index-strategy unsafe-best-match` with an explicit ROCm extra index so PyTorch ROCm wheels could resolve alongside non-ROCm transitive packages from PyPI.
- Task 2 kept the runtime taxonomy in one shared backend service by splitting raw fact collection (`runtime_profile.py`) from mismatch derivation (`runtime_diagnostics.py`), which lets later doctor/API/UI tasks reuse the same issue vocabulary.
- Stable payload additions for downstream tasks are `accelerator.torch_build_family`, optional `accelerator.device_name`, full-payload `issues[]`, and summary-level `issue_codes`; the structured mismatch codes now in use are `wrong_torch_build_cuda_on_wsl`, `cpu_only_torch_on_wsl`, and `hip_build_no_device`.
- Task 3 implemented the strict doctor as a backend-owned module (`app.runtime_doctor`) that reuses `detect_runtime_profile()` plus `derive_runtime_diagnostics()` and emits a stable success marker `WSL_ROCM_READY`, so later smoke/docs work can consume the same mismatch vocabulary and pass/fail contract without depending on the API server.
- The `scripts/check_wsl_rocm.sh` wrapper also needs the executable bit tracked in the repository; valid shell syntax alone is not enough for the QA path because the required operator invocation is `./scripts/check_wsl_rocm.sh`.
2026-04-26
- Task 4 kept `/api/runtime/capabilities` verbatim but expanded the compact summary used by `/api/health` and startup logs to include `issue_codes` plus a minimal `accelerator` snapshot (`available`, `backend`, `device_count`, `device_name`, `torch_build_family`) so operators can distinguish WSL CUDA-wheel mismatches from generic cpu-only fallback without breaking the 200/summary contract.
- Worker preflight surfacing now uses a structured `worker_preflight_runtime=` log payload rather than a warnings-only string, reusing the same `issues`, `issue_codes`, and accelerator fields as the API/UI surfaces.
- `EnvironmentStatusCard` now keys its targeted WSL remediation copy off the shared `wrong_torch_build_cuda_on_wsl` issue code while preserving the healthy `wsl-rocm` satisfied state and existing non-blocking warning semantics for other degraded profiles.
- Task 5 kept the generic non-docker smoke host-agnostic by moving local stack lifecycle into `_release_smoke_common.sh`; the dedicated `release_smoke_wsl_rocm.sh` now runs `./scripts/check_wsl_rocm.sh` before startup, then waits until `/api/health` reports `runtime_capabilities.detected_profile == "wsl-rocm"` before reusing the existing downstream smoke suite.
- The dedicated WSL smoke wrapper also needs its executable bit tracked in git, just like `scripts/check_wsl_rocm.sh`, because the documented operator path is `./scripts/release_smoke_wsl_rocm.sh` rather than `bash scripts/release_smoke_wsl_rocm.sh`.
- Task 6 had to correct two stale doc assumptions at once: the WSL deployment path could no longer point at plain `uv sync --project backend --frozen`, and the runtime visibility docs had to move from `worker_preflight_warning=` plus a three-field `/api/health` summary to the shipped `worker_preflight_runtime=` payload with `issue_codes` and compact accelerator metadata.
- The operator-facing WSL contract is now documented as dedicated install plus doctor plus shared runtime entrypoints plus dedicated smoke, with the mismatch meanings for `wrong_torch_build_cuda_on_wsl`, `cpu_only_torch_on_wsl`, and `hip_build_no_device` explained consistently in both languages.
- Final docs repair also had to close the Linux + CUDA side of the same contract: because the repo now ships `./scripts/install_backend_linux_cuda.sh`, deployment and operator docs should describe Linux + CUDA backend setup through that dedicated wrapper instead of implying plain `uv sync --project backend --frozen` is the supported host install path.
- The strict WSL doctor output contract in docs must match `backend/app/runtime_doctor.py` exactly, including `torch.cuda.is_available=True` without parentheses.
- Final-wave backend repair: `detect_runtime_profile()`, capability import checks, and the doctor import probe all need to tolerate generic import-time exceptions from dependencies (not just `ModuleNotFoundError`) so a broken torch/transformers/whisperx install degrades into the existing unknown/error payload shapes instead of crashing, while still surfacing the original exception text for operators.
- Final-wave backend repair: the profile requirement artifacts must be compiled with the generated base lockfile as a constraints overlay input (after stripping the local `./backend` line) so shared packages stay pin-identical across `backend/requirements.txt` and `backend/requirements-*.txt`; otherwise `uv pip sync base profile` can receive conflicting duplicate pins such as `click`.

- 2026-04-26 manual-QA sufficiency review: targeted regressions, export check, runtime-doctor tests, startup tests, and UI status-card tests all passed; live `check_wsl_rocm.sh` and `release_smoke_wsl_rocm.sh` correctly failed fast on this host because torch is `2.8.0+cu128` with `torch.version.hip=None` and `torch.cuda.is_available=False`. Approval still requires one positive proof on a true healthy WSL ROCm host showing `WSL_ROCM_READY` and a successful `./scripts/release_smoke_wsl_rocm.sh` run reaching `runtime_capabilities.detected_profile == "wsl-rocm"`.

- Removed unrelated .sisyphus/boulder.json churn from the WSL ROCm deliverable diff so scope stays focused on implementation artifacts.

- Deployment guide wording repair: the full `/api/runtime/capabilities` contract should describe `issues` in the returned payload, while `issue_codes` stays reserved for compact summary surfaces such as `/api/health`, startup logs, and worker preflight summaries.
- Frontend smoke-fixture repair: `web/src/test/smoke.test.tsx` had drifted behind `RuntimeCapabilities` after the status-card contract started reading `capabilities.issues` and accelerator metadata; the warning-only cpu fixture now includes `issues: []`, `accelerator.device_name: null`, and `accelerator.torch_build_family: "cpu"` to preserve the same warning semantics without crashing `EnvironmentStatusCard`.
